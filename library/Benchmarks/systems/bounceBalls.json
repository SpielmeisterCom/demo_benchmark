{
	"version": 1,
	"type": "system",
	"dependencies": [
		"Benchmarks.entities.Ball",
		"spell.component.2d.transform"
	],
	"input": [
		{
			"name": "transformationComponents",
			"componentId": "spell.component.2d.transform"
		}
	],
	"config": [
		{
			"name": "active",
			"type": "boolean",
			"default": true,
			"doc": "if active is false the system will be skipped during processing"
		}
	]
}